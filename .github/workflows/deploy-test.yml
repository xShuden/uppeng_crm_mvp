name: Deploy CRM Frontend to Test Environment

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: xshuden/uppeng-crm-frontend
  DOCKER_HOST: ${{ secrets.DOCKER_HOST }}
  SERVICE_NAME: uppeng-crm-frontend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare deployment files
        run: |
          echo "📦 Preparing deployment files..."
          # Create a deployment archive  
          tar -czf /tmp/frontend-deploy.tar.gz -C . frontend/


      - name: Setup SSH for Remote Docker Access
        env:
          DOCKER_HOST: ${{ secrets.DOCKER_HOST }}
        run: |
          # For now, we'll build locally and deploy via direct SSH
          echo "Building Docker image locally, deploying via SSH"
          echo "Remote Docker host: $DOCKER_HOST"

      - name: Deploy to Server via SSH
        env:
          IMAGE_TAG: ${{ github.ref_name }}-${{ github.sha }}
        run: |
          echo "🚀 Deploying to server via SSH..."
          
          # Create SSH key file
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > /tmp/deploy_key
          chmod 600 /tmp/deploy_key
          
          # Upload frontend files to server
          scp -i /tmp/deploy_key -o StrictHostKeyChecking=no /tmp/frontend-deploy.tar.gz root@31.42.127.152:/tmp/
          
          # Deploy via SSH
          ssh -i /tmp/deploy_key -o StrictHostKeyChecking=no root@31.42.127.152 << EOF
            echo "📦 Extracting frontend files..."
            cd /tmp
            rm -rf frontend
            tar -xzf frontend-deploy.tar.gz
            
            echo "🔨 Building Docker image on server..."
            cd /tmp/frontend
            
            # Build Docker image locally on server
            docker build -t uppeng-crm-frontend:$IMAGE_TAG .
            
            echo "🗑️ Removing existing service if exists..."
            docker service rm uppeng-crm-frontend 2>/dev/null || echo "No existing service to remove"
            sleep 5
            
            echo "🚀 Creating new service..."
            docker service create \
              --name uppeng-crm-frontend \
              --publish 3000:3000 \
              --restart-condition any \
              --restart-max-attempts 3 \
              uppeng-crm-frontend:$IMAGE_TAG
            
            echo "🧹 Cleaning up..."
            rm -rf /tmp/frontend /tmp/frontend-deploy.tar.gz
            
            echo "✅ Service deployed successfully"
          EOF
          
          # Clean up SSH key
          rm -f /tmp/deploy_key

      - name: Verify Deployment
        run: |
          echo "⏳ Waiting for service to be ready..."
          sleep 30
          
          if [ ! -z "$DOCKER_HOST" ]; then
            # Check stack services
            echo "=== STACK SERVICES ==="
            docker stack services uppeng-crm-stack
            
            echo ""
            echo "=== RUNNING CONTAINERS ==="  
            docker ps --filter "name=uppeng-crm-stack"
            
            echo ""
            echo "=== SERVICE LOGS (last 20 lines) ==="
            docker service logs --tail 20 uppeng-crm-stack_uppeng-crm-frontend 2>/dev/null || echo "Logs not available yet"
          fi

      - name: Health Check
        run: |
          echo "🔍 Running health checks..."
          
          # Try to connect to the service
          MAX_ATTEMPTS=6
          ATTEMPT=1
          
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            echo "Health check attempt $ATTEMPT/$MAX_ATTEMPTS..."
            
            if curl -f -s http://test-8xpm.uppeng.co/health > /dev/null 2>&1; then
              echo "✅ Health check passed!"
              break
            else
              echo "⏳ Service not ready yet, waiting..."
              sleep 30
              ATTEMPT=$((ATTEMPT + 1))
            fi
          done
          
          if [ $ATTEMPT -gt $MAX_ATTEMPTS ]; then
            echo "⚠️  Health check failed after $MAX_ATTEMPTS attempts"
            echo "Service may still be starting up..."
          fi

      - name: Deployment Summary
        if: always()
        env:
          IMAGE_TAG: ${{ github.ref_name }}-${{ github.sha }}
        run: |
          echo "========================================"
          echo "🚀 CRM v2 Frontend Deployment Summary"
          echo "========================================"
          echo "📦 Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${IMAGE_TAG}"
          echo "🎯 Environment: Test"
          echo "🐳 Deployment: Docker Swarm Stack"
          echo "🌐 URL: http://test-8xpm.uppeng.co"
          echo "📋 Stack: uppeng-crm-stack"
          echo "🔧 Service: uppeng-crm-frontend"
          echo "========================================"